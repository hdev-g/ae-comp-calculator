services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: comp_calculator
      POSTGRES_PASSWORD: comp_calculator
      POSTGRES_DB: comp_calculator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

